<h2>Sign up</h2>
<div class="row-fluid" style="text-align: left">
  <div class="span4"></div>
    <div class="span4">
    <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
      
        <div class="alert alert-error">
          <a class="close" data-dismiss="alert" href="#">×</a>
          <%= devise_error_messages1!.html_safe %>
        </div>

      <div class="control-group">
        <%= f.label :email, class: "control-label"%>  
        <div class="controls">
          <%= f.email_field :email, :class => "span12", :autofocus => true %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :password, class: "control-label"%>  
        <div class="controls">
          <%= f.password_field :password, :class => "span12", :autofocus => true %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :password_confirmation, class: "control-label"%>  
        <div class="controls">
          <%= f.password_field :password_confirmation, :class => "span12", :autofocus => true %>
        </div>
      </div>

      <div><%= f.submit "Sign up", :class => "span4 btn pull-right" %></div>
    <% end %>
  </div>
  <div class="span4"></div>
</div>
<div class="registration-links">
  <%= link_to "Sign in", new_session_path(resource_name) %> | 
  <%= link_to "Sign in with Facebook", omniauth_authorize_path(resource_name, :facebook) %>
</div>
