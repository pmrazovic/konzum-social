<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="#">Konzum social</a>
      <div class="btn-group pull-right">
        <div class="nav-collapse">
          <% if current_user.blank? %>
            <% if params[:controller] == 'devise/sessions' and params[:action] == 'new' %>
              <%= link_to "Sign Up", new_registration_path(resource_name), :class => "btn btn-inverse" %>
            <% elsif params[:controller] == 'devise/registrations' and params[:action] == 'new' %>
              <%= link_to "Sign In", new_session_path(resource_name), :class => "btn btn-inverse" %>
            <% end %>
          <% else %>
            <a class="btn btn-inverse dropdown-toggle" data-toggle="dropdown" href="#">
              <i class="icon-user icon-white"></i> Username
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><a href="#"><%= t('.profile') %></a></li>
              <li class="divider"></li>
              <li><a href="#"><%= t('.sign_out')%></a></li>
            </ul>
          <% end %>
        </div>
      </div>

      <div class="nav-collapse">
        <ul class="nav">
          <li class="divider-vertical"></li>
          <li><%= link_to "Home", "#" %></li>
          <li class="divider-vertical"></li>
          <li><%= link_to "Products", "#" %></li>
          <li class="divider-vertical"></li>
          <li><%= link_to "Friends", "#" %></li>
          <li class="divider-vertical"></li>
          <li><%= link_to "Settings", "#" %></li>
          <li class="divider-vertical"></li>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>